<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.utng.wsmovie">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WebServerMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListMovies"
            android:label="List Movies" />
        <activity android:name=".MainActivity" />
        <activity android:name=".DocumentMain" />
        <activity android:name=".ListDocuments" />
        <activity android:name=".CompanyActivity" />
        <activity android:name=".ListCompanies" />
        <activity android:name=".MeActivity" />
        <activity android:name=".ListMine" />
        <activity android:name=".FruitActivity" />
        <activity android:name=".ListFruits" />
        <activity android:name=".SongActivity" />
        <activity android:name=".ListSongs"></activity>
    </application>

</manifest>